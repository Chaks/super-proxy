CREATE KEYSPACE IF NOT EXISTS k1 WITH replication = {'class':'SimpleStrategy', 'replication_factor':1};
CREATE TABLE IF NOT EXISTS k1.exchange(hash_code text, context text, payload text, response text, correlation_id text, PRIMARY KEY((hash_code), context));